
function categorize(oArgs)
{
    var requestUrl = phpGWLink('index.php', oArgs);

    window.open(requestUrl, '_self');
}

function add_vcard(oArgs)
{
    var requestUrl = phpGWLink('index.php', oArgs);

    window.open(requestUrl, '_self');
}

function import_contacts(oArgs)
{
    var requestUrl = phpGWLink('index.php', oArgs);

    window.open(requestUrl, '_self');
}

function export_contacts(oArgs)
{
    var requestUrl = phpGWLink('index.php', oArgs);

    window.open(requestUrl, '_self');
}