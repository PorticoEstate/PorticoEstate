CREATE TABLE phpgw_eldaptir_servers (
   id int(11) NOT NULL auto_increment,
   name varchar(32),
   type varchar(32),
   basedn varchar(64),
   rootdn varchar(64),
   rootpw varchar(64),
   is_default int(1) DEFAULT '0' NOT NULL,
   PRIMARY KEY (id)
);
